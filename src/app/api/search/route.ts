import{searchQuery}from"@/domain/search/structured";import{authorizePersistedMatter}from"@/platform/access-policy-persistence";import{GOLDEN_MATTER_ID,PILOT_TENANT_ID,pilotContext}from"@/platform/pilot-context";import{requestActor}from"@/platform/request-actor";import{structuredMatterSearch}from"@/platform/structured-search-persistence";import{AuthorizationError}from"@/platform/tenant-context";export async function GET(request:Request){try{const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});const context=pilotContext(actor);await authorizePersistedMatter(context,PILOT_TENANT_ID,GOLDEN_MATTER_ID);const q=searchQuery.parse(new URL(request.url).searchParams.get("q")??"");return Response.json({query:q,results:await structuredMatterSearch(PILOT_TENANT_ID,GOLDEN_MATTER_ID,q),limitation:"This is tenant- and matter-scoped deterministic structured search. Ask Athena and AI retrieval remain disabled; results are direct records, not generated answers."});}catch(e){if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:e instanceof Error?e.message:"Search failed"},{status:400});}}
