import{decideTask,taskCommand}from"@/domain/work/tasks";import{authorizePersistedMatter}from"@/platform/access-policy-persistence";import{GOLDEN_MATTER_ID,PILOT_TENANT_ID,pilotContext}from"@/platform/pilot-context";import{enforceRateLimit,RateLimitError,rateLimitResponse}from"@/platform/rate-limit-persistence";import{requestActor}from"@/platform/request-actor";import{assertTrustedWriteOrigin,RequestSecurityError}from"@/platform/request-security";import{countIncompleteDependencies,listMatterTasks,persistTaskDecision,readTask,readTaskEventByIdempotency}from"@/platform/task-persistence";import{AuthorizationError}from"@/platform/tenant-context";export async function GET(request:Request){try{const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});const context=pilotContext(actor);await authorizePersistedMatter(context,PILOT_TENANT_ID,GOLDEN_MATTER_ID);return Response.json({items:await listMatterTasks(PILOT_TENANT_ID,GOLDEN_MATTER_ID)});}catch(e){if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:"Tasks could not be read"},{status:500});}}export async function POST(request:Request){try{assertTrustedWriteOrigin(request);const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});await enforceRateLimit({tenantId:PILOT_TENANT_ID,actorId:actor.userId,action:"tasks.command",policy:{limit:20,windowMs:60_000}});const context=pilotContext(actor),command=taskCommand.parse(await request.json());await authorizePersistedMatter(context,command.tenantId,command.matterId);const prior=await readTaskEventByIdempotency(command.tenantId,command.idempotencyKey);if(prior)return Response.json({event:null,persistence:{replayed:true,eventId:prior.eventId},items:await listMatterTasks(command.tenantId,command.matterId)});const current=command.action==="create"?null:await readTask(command.tenantId,command.matterId,command.taskId),incompleteDependencyCount=command.action==="create"?0:await countIncompleteDependencies(command.tenantId,command.matterId,command.taskId),decision=decideTask({context,raw:command,current,incompleteDependencyCount}),persistence=await persistTaskDecision({...decision,actor});return Response.json({event:decision.event,persistence,items:await listMatterTasks(command.tenantId,command.matterId)});}catch(e){if(e instanceof RateLimitError)return rateLimitResponse(e);if(e instanceof RequestSecurityError)return Response.json({error:"Untrusted request origin"},{status:403});if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:e instanceof Error?e.message:"Task command failed"},{status:400});}}
