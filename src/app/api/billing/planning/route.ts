import{budgetPlanningCommand,decideBudgetPlanning,type BudgetState}from"@/domain/billing/planning";import{authorizePersistedMatter}from"@/platform/access-policy-persistence";import{budgetProjection,persistBudgetPlanning,readBudget,readBudgetEvent}from"@/platform/budget-planning-persistence";import{GOLDEN_MATTER_ID,PILOT_TENANT_ID,pilotContext}from"@/platform/pilot-context";import{enforceRateLimit,RateLimitError,rateLimitResponse}from"@/platform/rate-limit-persistence";import{requestActor}from"@/platform/request-actor";import{assertTrustedWriteOrigin,RequestSecurityError}from"@/platform/request-security";import{AuthorizationError}from"@/platform/tenant-context";const limitation="Budget, accrual, realization, and contribution figures are deterministic synthetic planning evidence using integer cents. No accounting, e-billing, payroll, or client-budget provider is connected.";export async function GET(request:Request){try{const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});await authorizePersistedMatter(pilotContext(actor),PILOT_TENANT_ID,GOLDEN_MATTER_ID);return Response.json({...await budgetProjection(PILOT_TENANT_ID,GOLDEN_MATTER_ID),limitation})}catch(e){if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:"Budget planning could not be read"},{status:500})}}export async function POST(request:Request){try{assertTrustedWriteOrigin(request);const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});await enforceRateLimit({tenantId:PILOT_TENANT_ID,actorId:actor.userId,action:"billing_planning.command",policy:{limit:20,windowMs:60_000}});const context=pilotContext(actor),command=budgetPlanningCommand.parse(await request.json());await authorizePersistedMatter(context,command.tenantId,command.matterId);const prior=await readBudgetEvent(command.tenantId,command.idempotencyKey);if(prior)return Response.json({event:null,persistence:{replayed:true,eventId:prior.eventId},...await budgetProjection(command.tenantId,command.matterId),limitation});const budget=await readBudget(command.tenantId,command.matterId,command.budgetId),decision=decideBudgetPlanning({context,raw:command,budget:budget as BudgetState|null,targetExists:Boolean(budget)}),persistence=await persistBudgetPlanning({...decision,actor});return Response.json({event:decision.event,persistence,...await budgetProjection(command.tenantId,command.matterId),limitation})}catch(e){if(e instanceof RateLimitError)return rateLimitResponse(e);if(e instanceof RequestSecurityError)return Response.json({error:"Untrusted request origin"},{status:403});if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:e instanceof Error?e.message:"Budget planning command failed"},{status:400})}}
