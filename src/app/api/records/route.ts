import{decideRecordRequest,recordRequestCommand}from"@/domain/records/request";import{authorizePersistedMatter}from"@/platform/access-policy-persistence";import{GOLDEN_MATTER_ID,PILOT_TENANT_ID,pilotContext}from"@/platform/pilot-context";import{enforceRateLimit,RateLimitError,rateLimitResponse}from"@/platform/rate-limit-persistence";import{listRecordRequests,persistRecordRequest,readRecordRequest,readRecordRequestEvent}from"@/platform/record-request-persistence";import{requestActor}from"@/platform/request-actor";import{assertTrustedWriteOrigin,RequestSecurityError}from"@/platform/request-security";import{AuthorizationError}from"@/platform/tenant-context";export async function GET(request:Request){try{const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});const context=pilotContext(actor);await authorizePersistedMatter(context,PILOT_TENANT_ID,GOLDEN_MATTER_ID);return Response.json({items:await listRecordRequests(PILOT_TENANT_ID,GOLDEN_MATTER_ID),limitation:"Human-recorded deterministic lifecycle only. No subpoena vendor, process server, OCR, expert delivery, or billing provider is connected."});}catch(e){if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:"Record requests could not be read"},{status:500});}}export async function POST(request:Request){try{assertTrustedWriteOrigin(request);const actor=requestActor(request);if(!actor)return Response.json({error:"Authentication required"},{status:401});await enforceRateLimit({tenantId:PILOT_TENANT_ID,actorId:actor.userId,action:"records.command",policy:{limit:20,windowMs:60_000}});const context=pilotContext(actor),command=recordRequestCommand.parse(await request.json());await authorizePersistedMatter(context,command.tenantId,command.matterId);const prior=await readRecordRequestEvent(command.tenantId,command.idempotencyKey);if(prior)return Response.json({event:null,persistence:{replayed:true,eventId:prior.eventId},items:await listRecordRequests(command.tenantId,command.matterId)});const current=command.action==="create"?null:await readRecordRequest(command.tenantId,command.matterId,command.recordRequestId),decision=decideRecordRequest({context,raw:command,current}),persistence=await persistRecordRequest({...decision,actor});return Response.json({event:decision.event,persistence,items:await listRecordRequests(command.tenantId,command.matterId)});}catch(e){if(e instanceof RateLimitError)return rateLimitResponse(e);if(e instanceof RequestSecurityError)return Response.json({error:"Untrusted request origin"},{status:403});if(e instanceof AuthorizationError)return Response.json({error:"Access denied"},{status:403});return Response.json({error:e instanceof Error?e.message:"Record request command failed"},{status:400});}}
