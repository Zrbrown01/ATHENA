import{cleanup,fireEvent,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it}from"vitest";import{CommandPalette}from"./command-palette";afterEach(cleanup);describe("CommandPalette",()=>{it("opens from the trigger, exposes a labeled dialog/input, and closes with Escape",()=>{render(<CommandPalette/>);const trigger=screen.getByRole("button",{name:/search matters/i});fireEvent.click(trigger);expect(screen.getByRole("dialog",{name:"Search Athena"})).toBeInTheDocument();expect(screen.getByLabelText("Search query")).toHaveFocus();expect(screen.getByText(/AI disabled/)).toBeInTheDocument();fireEvent.keyDown(window,{key:"Escape"});expect(screen.queryByRole("dialog")).not.toBeInTheDocument()});it("opens with the platform keyboard shortcut",()=>{render(<CommandPalette/>);fireEvent.keyDown(window,{key:"k",metaKey:true});expect(screen.getByRole("dialog",{name:"Search Athena"})).toBeInTheDocument()})});
